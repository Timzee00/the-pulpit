# the-pulpit
It's a form-driven prompt builder that sends structured instructions to an LLM API, parses the JSON response, and dynamically renders a complete sermon UI — no backend, no database, just HTML, CSS, JS, and an API call
